--Remove Duplicate Records (keep latest)

if OBJECT_ID('tempdb..#Employee') is not null
	drop table #Employee

CREATE TABLE #Employee
(
    Id INT PRIMARY KEY,
    Name VARCHAR(100),
    Email VARCHAR(100)
);

INSERT INTO #Employee (Id, Name, Email)
VALUES
(1, 'Anurag', 'anurag@gmail.com'),
(2, 'Rahul', 'rahul@gmail.com'),
(3, 'Amit', 'amit@gmail.com'),
(4, 'Neha', 'rahul@gmail.com'),
(5, 'Priya', 'priya@gmail.com'),
(6, 'Karan', 'anurag@gmail.com');

 
;with cte as(
	select id,email,row_number()over(partition by email order by id desc) as duplicate
	from #Employee
) 


delete from cte where id in (select id from cte where duplicate>1)

select * from #Employee  
